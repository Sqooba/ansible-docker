---

# docker group gid
docker_group_gid: 992

# Whether to start docker service or not after installing the binaries
docker_start_daemon: True

# Whether to enable the docker daemon to start at next reboot
docker_enable_daemon: True

docker_data_dir: /data/docker
docker_storage_driver: devicemapper

docker_version: "19.03.*"

docker_package_name: "docker-ce"

docker_unsecure_registries: []

docker_default_address_pools:
  base: "198.19.0.0/16"
  size: 24
